const size = {
   mm: '375px',
   ml: '425px',
   tablet : '768px'
}

export const Devices = {
   mm : `(max-width: ${size.mm})`,
   ml: `(max-width: ${size.ml})`,
   tablet: `(max-width: ${size.tablet})`
}